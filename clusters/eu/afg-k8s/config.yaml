# Cluster registry entry — see clusters/us/nv-k8s/config.yaml for the contract.
# AFG-k8s is the EU hub AND workload cluster: its own ArgoCD manages it via a
# named in-cluster Secret ("afg-k8s" -> kubernetes.default.svc).
cloud: aws
role: primary          # the EU primary
