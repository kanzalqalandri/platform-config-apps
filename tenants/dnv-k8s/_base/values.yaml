# Cluster-wide defaults for ALL tenants on dnv-k8s.
# Sits above the global tenants/_base and below each tenant's own values.yaml.
# Example: raise the baseline resource requests for everything on this cluster.
resources:
  requests:
    cpu: 50m
    memory: 48Mi
